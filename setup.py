from setuptools import setup

setup(
   name='sparkDiffusionMap',
   version='1.0',
   description='A module for spark diffusion mapping',
   author='Joe Slade',
   author_email='jfsladeIV@gmail.com',
   packages=['sparkDiffusionMap'],
   install_requires=['numpy', 'scipy'], #external packages as dependencies
)
